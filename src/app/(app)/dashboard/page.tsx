import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Dashboard',
  description: 'Your carbon health score, category breakdown, and daily streak.',
};

export { default } from './DashboardClient';
